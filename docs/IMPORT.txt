The VBA import guide lives next to the modules it describes:

  VBA\IMPORT.txt

(Import order, ThisOutlookSession setup, install root, dialogs, ribbon host,
and the aes\ / guri\ script locations.)
